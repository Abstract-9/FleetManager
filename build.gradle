plugins {
    id 'application'
}

group 'com.jamz'
version '0.1'

repositories {
    mavenCentral()
}

tasks.register("runAll") {
    dependsOn('job-manager:run', 'drone-manager:run', 'flight-path-manager:run', 'landing-bay-manager:run')
}